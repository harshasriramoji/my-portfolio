# No build command needed - serving pre-built dist folder as static site
[build]
publish = "client/dist"

# Redirect all routes to index.html for SPA (Single Page Application)
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
